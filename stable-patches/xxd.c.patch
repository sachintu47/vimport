diff --git a/src/xxd/xxd.c b/src/xxd/xxd.c
index 9b1ca6ea5..32a1f093b 100644
--- a/src/xxd/xxd.c
+++ b/src/xxd/xxd.c
@@ -122,7 +122,9 @@
 # include <unix.h>	/* for fdopen() on MAC */
 #endif
 
-
+#ifdef __MVS__
+# include <zos.h>
+#endif
 /*  This corrects the problem of missing prototypes for certain functions
  *  in some GNU installations (e.g. SunOS 4.1.x).
  *  Darren Hiebert <darren@hmi.com> (sparc-sun-sunos4.1.3_U1/2.7.2.2)
@@ -1038,6 +1040,11 @@ main(int argc, char *argv[])
 	color = 0;
     }
 
+#ifdef __MVS__
+    // Disable auto-conversion on input file descriptors
+    __disableautocvt(fileno(fp));
+#endif
+
   if (revert)
     switch (hextype)
       {
